import React from "react";
import { Slide, Text , SlideProps } from "spectacle";

export const Slide13 = (props: SlideProps) => (
  <Slide {...props}>
    <Text>Why do we need to write</Text>
    <Text bold>React code twice?</Text>
  </Slide>
)

export default undefined
